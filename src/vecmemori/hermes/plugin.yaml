name: vecmemori
version: 0.1.0
description: "vecmemori — local SQLite fact memory with FTS5 + neural embedding retrieval, trust scoring, Japanese tokenization support, and Hermes Agent integration."
hooks:
  - on_session_end
